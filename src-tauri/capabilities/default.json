{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal grants for the main window. App-defined commands (get_app_info, db_health, extract_receipt) need no permission. The OCR plugin command and the file-open dialog (for picking a receipt image, FR-2.1) are granted. Remaining fs/biometric grants are added per-feature when the frontend actually calls them (new-feature skill) — keep this list minimal.",
  "windows": ["main"],
  "permissions": ["core:default", "ocr:default", "dialog:allow-open"]
}
